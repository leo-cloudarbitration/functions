name: cloud_googleads_hour

on:
  workflow_dispatch:  # Permite execução manual
  schedule:
    # Executa a cada hora no minuto 58
    - cron: "58 * * * *"

jobs:
  collect-google-ads-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip'  # Cache automático baseado em requirements.txt

      - name: 📦 Instalar dependências
        working-directory: google_ads/cloud_googleads_hour
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: 🔍 Verificar versões instaladas
        run: |
          echo "=== Versões das bibliotecas principais ==="
          python -c "
          import pkg_resources
          import grpc
          import google.api_core
          import google.cloud.bigquery
          try:
              ga_version = pkg_resources.get_distribution('google-ads').version
              print(f'✅ google-ads: {ga_version}')
          except:
              print('⚠️ google-ads: versão não detectada')
          print(f'✅ grpcio: {grpc.__version__}')
          print(f'✅ google-api-core: {google.api_core.__version__}')
          print(f'✅ google-cloud-bigquery: {google.cloud.bigquery.__version__}')
          "
          echo "=========================================="

      - name: 🚀 Executar coleta de dados
        working-directory: google_ads/cloud_googleads_hour
        env:
          SECRET_GOOGLE_SERVICE_ACCOUNT: ${{ secrets.SECRET_GOOGLE_SERVICE_ACCOUNT }}
          SECRET_GOOGLE_ADS_CONFIG: ${{ secrets.SECRET_GOOGLE_ADS_CONFIG }}
          # Variáveis de ambiente para otimizar GRPC
          GRPC_ENABLE_FORK_SUPPORT: "1"
          GRPC_POLL_STRATEGY: "poll"
          GRPC_PYTHON_LOG_LEVEL: "ERROR"
        run: |
          python main.py

      - name: ✅ Resultado
        if: success()
        run: |
          echo "🎉 Dados coletados e enviados ao BigQuery com sucesso!"
          
      - name: ❌ Tratamento de erro
        if: failure()
        run: |
          echo "❌ Falha na coleta de dados. Verifique os logs acima."
          exit 1
