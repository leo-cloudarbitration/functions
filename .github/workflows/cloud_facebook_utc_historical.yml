name: cloud_facebook_utc_historical

on:
  schedule:
    # Executa todo dia às 00:58 BRT (03:58 UTC)
    # Coleta dados de anteontem e adiciona ao histórico (APPEND)
    - cron: "58 3 * * *"
  workflow_dispatch: {}  # Permite execução manual

jobs:
  run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: facebook_ads/cloud_facebook_utc_historical

    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      - name: 🐍 Configurar Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: 'pip'

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 🔐 Autenticar no Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.SECRET_GOOGLE_SERVICE_ACCOUNT }}

      - name: 🚀 Executar coleta de dados históricos UTC (anteontem)
        env:
          SECRET_FACEBOOK_GROUPS_CONFIG_UTC: ${{ secrets.SECRET_FACEBOOK_GROUPS_CONFIG_UTC }}
          SECRET_GOOGLE_SERVICE_ACCOUNT: ${{ secrets.SECRET_GOOGLE_SERVICE_ACCOUNT }}
        run: |
          python main.py

      - name: ✅ Resultado
        if: success()
        run: |
          echo "🎉 Dados históricos UTC (anteontem) coletados e adicionados ao BigQuery com sucesso!"

      - name: ❌ Tratamento de erro
        if: failure()
        run: |
          echo "❌ Falha na coleta de dados históricos UTC. Verifique os logs acima."
          exit 1

