name: Debug BigQuery Connection

on:
  workflow_dispatch: {}

jobs:
  debug:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cloud_function_deploy

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
            python -m pip install --upgrade pip
            pip install -r requirements.txt

      - name: Configure GCP credentials
        run: echo "${{ secrets.SECRET_GOOGLE_SERVICE_ACCOUNT}}" > $HOME/gcp-key.json

      - name: Debug BigQuery
        env:
          GOOGLE_APPLICATION_CREDENTIALS: $HOME/gcp-key.json
        run: python debug_bigquery.py
